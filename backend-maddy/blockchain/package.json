{
    "name": "heartchain-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for HeartChain",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.js --network shardeum"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0"
    },
    "dependencies": {
        "dotenv": "^16.3.1"
    }
}